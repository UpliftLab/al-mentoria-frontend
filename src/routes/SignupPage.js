import SignupForm from '../features/user/SignupFrom';

const SignunPage = () => (
  <section className="h-full w-full flex justify-center items-center bg-lime-500">
    <SignupForm />
  </section>
);

export default SignunPage;
